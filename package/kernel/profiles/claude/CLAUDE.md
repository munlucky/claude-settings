# Moon Relay Kernel

Use the `moon-relay-kernel` entrypoint and the current work unit directly. Stay inside its allowed paths. Add no extra process, verification pass, or reviewer unless the Kernel action requires it. Report concrete changes, evidence, and blockers; only the Kernel completion decision can finish the run.
