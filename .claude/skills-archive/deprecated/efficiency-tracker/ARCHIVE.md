# Archived Skill: efficiency-tracker

Archived: 2026-04-24

## Reason

`efficiency-tracker` is no longer part of the public workflow surface or default Moonshot stage model. It overlapped with scorecard, QA, and handoff evidence without driving a required gate.

## Replacement Path

- Use `session-logger` for durable session notes.
- Use `SCORECARD.md` for objective completion state.
- Use `QA_REPORT.md` and verification verdict artifacts for evidence.

## Restore Procedure

Move this directory back to `.claude/skills/efficiency-tracker` only after assigning a non-deprecated `surfaceStatus` and documenting the exact bundle or utility path that owns it.
