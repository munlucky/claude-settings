# RSC props 중복 직렬화 피하기

- RSC -> client 직렬화는 값이 아니라 참조 기준 dedup이므로, 같은 데이터를 새 배열/객체로 변환해 여러 prop으로 넘기면 중복 전송이 늘어납니다.
- 정렬, 필터, map 같은 파생 변환은 가능한 한 client 쪽에서 수행합니다.
- 비싼 변환이거나 원본이 필요 없을 때만 예외적으로 서버에서 넘깁니다.

