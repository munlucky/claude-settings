# 컴포넌트 조합으로 서버 데이터 병렬 fetch

- Server Component 트리는 구조에 따라 순차 실행될 수 있으므로, fetch를 병렬화할 수 있게 컴포넌트 구조를 재배치합니다.
- `Page` 안에서 하나를 다 기다린 뒤 자식을 렌더하는 구조보다, 각각 독립 컴포넌트가 직접 fetch하게 만드는 편이 좋습니다.
- 핵심은 "트리 구조가 워터폴을 만들지 않게 설계"하는 것입니다.

