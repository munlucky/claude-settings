# Runtime TODO

- Claude runtime remains pending on this machine because the Claude subscription/access is not available yet.
- When access is enabled, rerun:
  - `bash .claude/scripts/verify-phase-runtime-parity.sh docs/implementation`
  - `bash .claude/agents/verification/verify-changes.sh`
