{
  "schemaVersion": 1,
  "catalogId": "moon-relay-kernel-skill-catalog.v1",
  "capabilityConditionsRevision": "kernel-capability-conditions.v1",
  "authority": {
    "publicRuntimeSurface": "package/kernel/manifest.json",
    "packageContract": "package/kernel/manifest.json",
    "sourceSkillRoot": "skills",
    "serviceProfilePolicy": "allowlist_only",
    "commonPayloadPolicy": "preserve_all_kernel_skills"
  },
  "requiredDocumentation": {
    "files": [
      "README.md"
    ]
  },
  "publicEntrypoints": [
    {
      "name": "moon-relay-kernel",
      "stage": "kernel",
      "ownerCluster": "kernel-control-plane",
      "engineRoute": "kernel-workflow",
      "invocationMode": "user_or_model",
      "allowedStages": ["FRAME", "EXECUTE", "PROVE", "CLOSE"],
      "conditionalSkillGroups": {
        "minimalChange": ["kernel-minimal-correct-change"],
        "verificationRequired": ["kernel-verification-before-completion"]
      },
      "source": "skills/moon-relay-kernel/SKILL.md",
      "purpose": "Kernel control plane entrypoint for adaptive risk-bound workflow."
    }
  ],
  "internalDiscovery": {
    "sourceRoot": "skills",
    "includeUnlistedSourceSkills": true,
    "profileLocalExposure": "forbidden"
  },
  "internalSkillClusters": [
    {
      "id": "kernel-internal-skills",
      "stage": "kernel",
      "skills": [
        "kernel-minimal-correct-change",
        "kernel-domain-modeling",
        "kernel-tracer-slicing",
        "kernel-test-driven-development",
        "kernel-diagnosing-bugs",
        "kernel-conditional-frontend-guidance",
        "kernel-browser-proof-adapter",
        "kernel-security-review-policy",
        "kernel-simplification-check",
        "kernel-verification-before-completion",
        "kernel-review-spec",
        "kernel-review-standards",
        "kernel-review-complexity",
        "kernel-commit-closeout"
      ]
    }
  ],
  "capabilityConditions": {
    "kernel-minimal-correct-change": { "condition": "sourceMutation or behaviorChanging", "priority": 10, "guidance": "Keep the change minimal and contract-shaped." },
    "kernel-domain-modeling": { "condition": "ambiguityChangesOutcome or domainTerminologyConflict", "priority": 20, "guidance": "Resolve domain ambiguity before implementation." },
    "kernel-tracer-slicing": { "condition": "taskClass=long-running or complex or safeParallelSplit or independentDeliverables", "priority": 30, "guidance": "Trace complex work and use the Step Ledger only when decomposition is explicit." },
    "kernel-test-driven-development": { "condition": "behaviorChanging (internal signal only; model guidance is compiled into kernel-focused-test-guidance)", "priority": 40, "guidance": "Use a failing test and a focused green implementation." },
    "kernel-diagnosing-bugs": { "condition": "taskClass=bug", "priority": 41, "guidance": "Identify the failure boundary before making the fix." },
    "kernel-review-spec": { "condition": "riskTier=T2/T3 or publicContract or acceptanceAmbiguity", "priority": 50, "guidance": "Review the public contract and acceptance semantics." },
    "kernel-review-standards": { "condition": "T1+ behaviorChanging", "priority": 51, "guidance": "Review implementation against applicable engineering standards." },
    "kernel-review-complexity": { "condition": "complex or newDependency", "priority": 60, "guidance": "Review complexity and dependency surface after functional review." },
    "kernel-commit-closeout": { "condition": "gitCloseoutRequested and completionAccepted and knowledgeCommitReceiptExists", "priority": 100, "guidance": "Run explicit approved Git closeout after knowledge commit." },
    "kernel-verification-before-completion": { "condition": "route includes EXECUTE or explicit completion attempt", "priority": 90, "guidance": "Require fresh, scope-matched evidence before completion." },
    "kernel-requirements-analysis": { "condition": "acceptance unverifiable, objective/non-goal conflict, or outcome-changing ambiguity", "priority": 21, "guidance": "Clarify only the contract details that change a verifiable outcome." },
    "kernel-conditional-design": { "condition": "architecture boundary, public contract, migration, or irreversible decision", "priority": 22, "guidance": "Record a short design judgment only when the decision changes the implementation boundary." },
    "kernel-conditional-planning": { "condition": "multiple independent deliverables, long-lived resume, or safe parallel split", "priority": 31, "guidance": "Use contract steps only when each slice has independent scope and verification." },
    "kernel-focused-test-guidance": { "condition": "behavior change with an appropriate test surface", "priority": 42, "guidance": "Use a focused regression test when it materially improves confidence in the changed behavior." },
    "kernel-systematic-debugging": { "condition": "repeated verification failure, repeated blocker, or unclear root cause", "priority": 43, "guidance": "Repeated attempts are failing. Re-evaluate the root cause before another patch." },
    "kernel-conditional-frontend-guidance": { "condition": "frontend source mutation, UI redesign, or visual behavior change", "priority": 44, "guidance": "Preserve design context, tokens, responsive behavior, accessibility, and state design." },
    "kernel-security-review-policy": { "condition": "authentication, untrusted input, dependency, security API, or high-risk data boundary", "priority": 52, "guidance": "Require project security verification and Kernel-recorded independent Review Receipt." },
    "kernel-simplification-check": { "condition": "new abstraction, new dependency, duplicate implementation, increased configuration surface, or broad change scope", "priority": 61, "guidance": "Remove unnecessary abstraction or scope expansion introduced by this change." },
    "kernel-browser-proof-adapter": { "condition": "browser behavior is explicit, required, or necessary to judge completion", "priority": 91, "guidance": "Use the Kernel browser proof adapter only when the contract requires browser behavior; enforce evidence depth." }
  },
  "checks": {
    "publicEntrypointsMatchRuntimeSurface": true,
    "publicEntrypointsMatchPackageContract": true,
    "publicEntrypointsDocumented": true,
    "serviceProfilesExposeOnlyPublicEntrypoints": true,
    "commonPayloadIncludesCatalog": true
  }
}
