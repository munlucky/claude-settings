schemaVersion: 1
reviewedPackageRoot: docs/implementation/moonshot-relay-contract-drift-remediation-2026-06-06
iteration: 2
reviewMode: parent-synthesis-after-independent-loops
blockingFindings: []
acceptedChanges:
  - Phase 01 now owns verification contract, Codex config, and Claude PROJECT runtime path breakage.
  - Phase 04 keeps plugin manifests as metadata and materializer dry-run as payload authority.
  - Phase 05 separates PS1 account-root wrapper, runtime state precedence, browser root selection, bin wrapper dry-run, and install guidance priority.
  - Phase 06 narrows public guideline work to durable minimum structure and moves deep rewrites out of scope.
rejectedChanges:
  - change: Run live account-root installer as part of planning.
    reason: Plan writing should not mutate account-root runtime state; verification uses dry-run/temp homes.
  - change: Directly update generated package profiles in source.
    reason: Generated profiles are verification output and remain untracked.
remainingAmbiguity:
  - If CI uses bare node --test, legacy archive test separation must include CI command updates.
  - If downstream consumers parse documentPaths.guidelinesRoot as profile-local runtime path, compatibility alias or migration note is required.
readinessDecision: pass
