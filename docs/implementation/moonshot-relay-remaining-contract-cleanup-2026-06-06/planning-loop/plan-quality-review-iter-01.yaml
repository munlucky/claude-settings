schemaVersion: 1
iteration: 1
reviewedPackageRoot: docs/implementation/moonshot-relay-remaining-contract-cleanup-2026-06-06
status: blocked_edits_applied
reviewerTypes:
  - readme-installer-runtime-docs
  - active-test-side-effect-archive-boundary
  - package-contract-manifest-boundary
  - skill-agent-profile-path-wording
  - public-guideline-depth-policy
blockingFindings:
  - reviewer: readme-installer-runtime-docs
    finding: README drift guards were not mapped one stale pattern at a time; broad rg scans could reject valid boundary wording; temp-home dry-run command was not concrete.
  - reviewer: active-test-side-effect-archive-boundary
    finding: Side-effect verification used global absence checks despite existing local runtime artifacts; Git Bash and WSL launcher were not distinguished; active/archive pass-fail scan was not automatic.
  - reviewer: package-contract-manifest-boundary
    finding: Broad plugin entries mitigation was undecided; Codex runtime exclusions were not required in both metadata and builder/tests.
  - reviewer: skill-agent-profile-path-wording
    finding: Owned paths missed multiple scan candidates and pass/fail logic did not define compatibility-context allowance.
acceptedChanges:
  - Added README drift guard matrix with exact stale pattern, replacement class, and test evidence.
  - Replaced broad README rg gates with exact stale-pattern gates and added temp-home dry-run command.
  - Changed side-effect acceptance from artifact absence to pre/post snapshot and temp-env isolation.
  - Required Git Bash/MSYS detection and Node-level fallback for WSL/no-Bash cases.
  - Fixed package manifest strategy to remove or replace broad consumer-facing scripts entries; warning-only is not enough.
  - Required Codex runtime exclusions in contract metadata, builder behavior, and package tests.
  - Expanded Phase 04 owned paths and added rewrite-vs-compatibility classification rules.
  - Clarified public guideline authority belongs to durable public/source docs, not phase plans.
rejectedChanges: []
remainingAmbiguity:
  - Whether Phase 05 executes now as a policy classification phase or remains optional after R-01 through R-15 are closed.
  - Whether existing local runtime artifacts should later get a separate cleanup phase; this package now treats them as baseline for snapshot comparison.
