# MemoryGraph Workflow KO

MemoryGraph refresh, promotion, generated-state boundary의 canonical source guideline입니다.

프로젝트 MemoryGraph refresh는 현재 코드베이스를 인덱싱하고 generated graph state는 tracked source 밖에 씁니다.
명시적 compatibility override가 없으면 새 write는 account-root project knowledge state를 사용합니다.
재사용 가능한 harness memory 승격은 명시 승인, redaction, 짧은 재발 방지 lesson이 필요합니다.
commit workflow가 memory refresh를 실행해도 memory artifact는 사용자가 포함 요청하기 전까지 stage하지 않습니다.
