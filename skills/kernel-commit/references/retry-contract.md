# Retry Contract

Git success and knowledge failure are recorded as a partial Saga closeout. Retry identity is `projectId:commitHash:candidateDigest`; a completed retry is idempotent. Push completion is local/remote HEAD parity, not command exit status alone.
