# Knowledge Closeout

An accepted Kernel Run may use Kernel completion authority. A run-less commit can create and stage candidates, but Project Knowledge changes require an explicit user-approved import transaction.
