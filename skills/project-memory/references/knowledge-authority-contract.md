# Knowledge Authority Contract

Kernel Run knowledge closeout requires accepted completion authority. Standalone import requires an explicit approval reference and a CAS-protected knowledge revision. The transaction is project-scoped, idempotent by source digest, and produces a receipt without creating a Run or completion decision.
