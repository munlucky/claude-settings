# Codebase Index Contract

The Code Index is a regenerable account-root artifact under `~/.moon-relay-kernel/state/projects/<projectId>/codebase`. It stores file and symbol graph data separately from reviewed Project Knowledge. A matching `sourceTreeDigest` and skill version is a cache hit; changed files and deleted files are reported by incremental refresh.
