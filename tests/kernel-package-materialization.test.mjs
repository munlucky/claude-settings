import assert from 'node:assert/strict'; import {test} from 'node:test'; import {mkdtemp} from 'node:fs/promises'; import os from 'node:os'; import path from 'node:path'; import {planKernelPackage} from '../scripts/kernel/package-build.mjs';
test('Kernel package dry plan contains only Kernel payload roots',async()=>{const out=await mkdtemp(path.join(os.tmpdir(),'kernel-pkg-')); const plan=await planKernelPackage({sourceRoot:path.resolve(new URL('..',import.meta.url).pathname),outputRoot:out}); assert.equal(plan.manifest.productId,'moon-relay-kernel'); assert.ok(plan.planned.every((p)=>!p.target.includes('.moonshot-relay')));});
