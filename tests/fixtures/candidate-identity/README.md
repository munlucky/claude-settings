# Candidate Identity Fixtures

This directory is reserved for reviewed candidate identity fixture payloads.

Fixtures committed here must avoid raw logs, transcripts, secrets, browser scrapes, or generated runtime database state.
