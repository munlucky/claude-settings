# Delivery Submit Fixtures

This directory is reserved for delivery submit receipt fixtures.

Tests create per-run JSON receipts in a temporary directory so repository state is not mutated after scoring.
