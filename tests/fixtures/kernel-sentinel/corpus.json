{
  "schemaVersion": 1,
  "taskSetRevision": "kernel-sentinel.v1",
  "description": "Fixed false-completion sentinel set. Each 'reject' case is a trap the Kernel must NOT accept; 'accept' controls verify the harness is not trivially blocking everything.",
  "cases": [
    { "id": "SEN-001", "trap": "caller_attested_only_mutating", "expect": "reject", "rationale": "Source mutated but only caller-attested proof supplied; needs kernel-runtime hard evidence." },
    { "id": "SEN-002", "trap": "stale_identity", "expect": "reject", "rationale": "Verification passed, then source mutated again; evidence is stale." },
    { "id": "SEN-003", "trap": "missing_obligation", "expect": "reject", "rationale": "A required obligation was never satisfied." },
    { "id": "SEN-004", "trap": "uncovered_acceptance", "expect": "reject", "rationale": "Acceptance criterion has no covering evidence." },
    { "id": "SEN-005", "trap": "no_verification_mutating", "expect": "reject", "rationale": "Mutating run closed with no verification at all." },
    { "id": "SEN-006", "trap": "protected_waiver", "expect": "reject", "rationale": "Protected obligation (auth) waived instead of proven." },
    { "id": "SEN-007", "trap": "failing_evidence", "expect": "reject", "rationale": "The only evidence is a failing verification." },
    { "id": "SEN-100", "trap": "clean_hard_evidence", "expect": "accept", "rationale": "Legitimate: mutating run with passing kernel-runtime hard evidence and covered acceptance." }
  ]
}
