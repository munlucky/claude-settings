import assert from 'node:assert/strict'; import {test} from 'node:test'; import {mkdtemp,writeFile,readFile} from 'node:fs/promises'; import os from 'node:os'; import path from 'node:path'; import {openKernelStateStore} from '../scripts/kernel/state-store.mjs';
test('existing Relay DB is not read or modified by Kernel store',async()=>{const root=await mkdtemp(path.join(os.tmpdir(),'krn-nomig-')); const relay=path.join(root,'.moonshot-relay'); const kernel=path.join(root,'.moon-relay-kernel'); await (await import('node:fs/promises')).mkdir(path.join(relay,'state'),{recursive:true}); const f=path.join(relay,'state','runtime-state.sqlite'); await writeFile(f,'relay-sentinel'); const s=await openKernelStateStore({runtimeHome:kernel,relayHome:relay}); s.createRun({runId:'r',objective:'x'}); s.close(); assert.equal(await readFile(f,'utf8'),'relay-sentinel');});
