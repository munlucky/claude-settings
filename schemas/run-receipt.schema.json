{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://moonshot-relay.local/schemas/run-receipt.schema.json",
  "title": "Run Receipt",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "artifactId", "candidate_id", "candidateId", "sourceDigest", "environmentDigest", "commands"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "artifactId": { "const": "RUN_RECEIPT" },
    "candidate_id": { "$ref": "#/$defs/candidateId" },
    "candidateId": { "$ref": "#/$defs/candidateId" },
    "sourceDigest": { "$ref": "#/$defs/sha256" },
    "environmentDigest": { "$ref": "#/$defs/sha256" },
    "policyDigest": { "$ref": "#/$defs/sha256" },
    "commands": { "type": "array", "items": { "$ref": "#/$defs/commandEvidence" } }
  },
  "$defs": {
    "candidateId": { "type": "string", "pattern": "^cand_[a-f0-9]{32}$" },
    "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "commandEvidence": {
      "type": "object",
      "additionalProperties": false,
      "required": ["argv", "cwd", "exitCode"],
      "properties": {
        "argv": { "type": "array", "items": { "type": "string" } },
        "cwd": { "type": "string" },
        "exitCode": { "type": "integer" },
        "stdoutDigest": { "$ref": "#/$defs/sha256" },
        "stderrDigest": { "$ref": "#/$defs/sha256" }
      }
    }
  }
}
